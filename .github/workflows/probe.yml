name: probe
on:
  workflow_dispatch:
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - env:
          PAT: ${{ secrets.GH_PAT }}
        run: |
          echo "GH_PAT length: ${#PAT}"
          echo "first char class: $(printf '%s' "$PAT" | head -c1 | tr -dc 'a-zA-Z_' | wc -c)"
